"""
#INPUT: String
#OUTPUT: LENGTH of longest substring with no repeated characters
#NOTE: Substring is contiguous, not subsequence
"""


class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        